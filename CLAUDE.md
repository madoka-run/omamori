# omamori プロジェクト・コンテキスト(Claude Code用)

## 開発の進め方(必ず守ること)

- **Git branch**:作業を1つ完了するたびに、必ず`git status`と`git branch`で今の状態を確認すること。新しいタスクに着手する前は、`main`から新しいブランチを切っているか確認すること。ブランチを切り忘れたまま作業を進めない。
- **コードの説明**:開発者はRuby on Rails初学者。書いたコード・使ったメソッドや構文について、簡単な説明を必ず添えること(「〜という意味です」「〜のために、この書き方を使っています」など)。専門用語を使う時は、一言補足する。
- **タスクの進め方**:
  1. まず、これから行うタスクの概要(何を・なぜ)を説明する
  2. 変更点を、一気に全部出すのではなく、1つずつ提案する
  3. 提案した1つの変更が、開発者によって確認・承認され、実際に反映・動作確認が完了してから、次のステップに進む
  4. 開発者からの承認を待たずに、複数の変更を先回りして進めない

## アプリ概要

**omamori**は、不安・パニックが起きたときに「この不安はいつか終わる」と思えるようにする、シンプルなWebアプリ。RUNTEQ「バトルオブランテック」個人ハッカソン提出物。

### コンセプト
- 不安中でも最低限の操作で使える、シンプルなUI
- 呼吸法を一緒に実践しながら、不安が始まってから終わるまでの時間を記録
- 過去の「不安が終わった記録」を振り返れる

### 技術スタック
- Ruby 3.2.2 / Rails 7.1.6
- PostgreSQL 14
- Docker Compose
- Stimulus.js(importmap)
- Devise(認証)

---

## これまでの実装内容

### 環境構築
- `docker-compose.yml`のボリュームマウント先を `/calm-wave` から `/rails` に修正済み(Dockerfileの`WORKDIR`と一致させた)
- タイムゾーンを `Tokyo` (JST) に設定済み(`config/application.rb`)
- `config.i18n.default_locale = :ja` を設定し、日本語をデフォルトに
- `Dockerfile`に `build-essential` を追加し、bcryptなどのnative extensionをビルド可能に(本番用の2段階ビルド構成の、最終ステージにも追加。本来は`Dockerfile.dev`に分離すべきだが未着手)

### 不安ログ記録機能
- `AnxietyLog`モデル:`anxiety_level`(1〜5)、`started_at`、`ended_at`、`user_id`
- `duration_in_minutes`(終了までの所要時間・分)、`elapsed_minutes`(現在までの経過時間・分)をモデルメソッドとして実装
- `AnxietyLogsController`:`new`/`create`/`check`/`update`/`finished`/`suggestions`/`index`/`destroy`
- トップページ(`/`)で不安レベル(1〜5)を選択 → 保存 → 呼吸法ページへ自動遷移

### 呼吸法機能
- `BreathingsController#show`、`BREATHING_METHODS`ハッシュに呼吸法データを定義(現状 `calming` のみ:吸う4秒・止める4秒・吐く4秒・5サイクル=1分)
- Stimulusコントローラ(`breathing_controller.js`)でフェーズ管理・タイマー・自動ループ
- 見た目:桜の花びら画像(`petal.png`)をCSS `transform`で放射状に配置。吸う/吐くで花びらが増減、止める間は`🫧`が増える演出
- 呼吸法終了後、`anxiety_log_id`をクエリパラメータとして引き継ぎながら `check` 画面へ自動遷移(2秒後)

### 確認・分岐フロー
- `check`画面:「不安は落ち着きましたか?」→「はい」で`ended_at`記録して`finished`へ、「まだ不安」で`suggestions`へ
- `suggestions`画面:TIPP・よりそいホットライン(0120-279-338、福島県は0120-279-226)・グラウンディングの提案リスト、呼吸法へ再度戻れるリンク、`check`へ戻れるリンク
- `finished`画面:不安レベル・所要時間を表示、新規登録/ログインへの導線(未ログイン時に、そのログを紐付けるため)

### 認証(Devise)
- `User`モデル。`:recoverable`(パスワードリセット)は、メール送信の実装コストに見合わないため削除済み
- `Users::RegistrationsController`と`Users::SessionsController`をカスタマイズし、新規登録・ログイン成功時に、URLパラメータで渡された`anxiety_log_id`を、そのユーザーに紐付ける処理を実装(仮ユーザー→本登録の橋渡し)
- ログイン後は `/anxiety_logs/new` にリダイレクト(`after_sign_in_path_for`)
- 履歴一覧(`index`)は、ログイン時は自分のログ、未ログイン時は`user_id`が`nil`のログのみ表示
- ブラウザバックで古いログイン状態が表示される問題を`Cache-Control: no-store`で解消
- Devise標準メッセージ(`devise.ja.yml`)とRails標準バリデーションメッセージ(`ja.yml`、rails-i18nから取得)を日本語化済み。Viewの英語文言(Sign up、Log inなど)も個別に日本語化済み

### Git運用
- ブランチを切り、PRを作成してマージする運用(GitHub: madoka-run/omamori)
- コミットメッセージは `feat:` `fix:` `chore:` プレフィックスを使用
- 一部、混乱で`main`に直接コミットした箇所があるが、基本はブランチ運用

**`check`画面を廃止し、フローを簡略化**
- 呼吸法終了時、その場で3択(「もう一度やる」「他の方法を試す」「落ち着いた」)を表示
- 「他の方法を試す」→ `suggestions`画面へ直接遷移
- 「落ち着いた」→ `update`アクション(`status: resolved`)を直接呼び、`finished`画面へ
- `suggestions`画面にも、「不安を終了する」ボタンを追加し、確認ポップアップ(`turbo_confirm`、原因判明・解決済み)→ `update`直接呼び出し → `finished`へ
- 上記が両方動作確認できたら、`check`アクション・View・関連ルーティングを削除
---

## 今後のタスク(優先順位順、2026/8/13時点)

### 高優先度


2. **タイマー表示の拡張(分・秒表示 + 隠しボタン)**
   - 呼吸法ページと`suggestions`画面に設置(`check`画面は滞在時間が短いため対象外と判断)
   - 既存の`elapsed_timer_controller.js`を拡張・使い回す
   - 「タイマーがあることで焦りを感じる人もいる」という配慮から、隠せるようにする

3. **`finished`画面の導線修正**
   - ログイン中でも「新規会員登録」「ログイン」が表示されてしまっている → `user_signed_in?`で出し分け
   - 過去ログ一覧(`anxiety_logs_path`)へのリンクが無い → 追加

### 中優先度
4. **コーピングメソッドのカード選択UI**
   - TIPP・ホットライン・グラウンディングを、カード形式にし、クリックで詳細説明を開閉(別ページ遷移は避け、JS開閉で実装)
   - ログへの対処法記録機能(`anxiety_log_methods`中間テーブル)は、情報過多になる懸念からMVP範囲外として見送り

5. **ビジュアルを桜からクローバーに変更**
   - RUNTEQ受講生からのフィードバック:緑も落ち着く色。四つ葉のクローバー=幸福の象徴という意味づけ
   - 画像アセット差し替え + CSS位置調整(桜のときと同様、試行錯誤が必要な見込み)

6. **不自然な`button_to`を`link_to`に戻す**
   - 状態変更を伴わない、単なる画面遷移(GET)なのに、見た目のために`button_to`にしてしまっている箇所がある
   - 対象:`app/views/breathings/show.html.erb`の「他の方法を試す」、`app/views/anxiety_logs/suggestions.html.erb`の「もう一度、呼吸法をやってみる」
   - `link_to` + CSSでボタン風の見た目にする方針(タスク7のリンクのボタン風デザイン統一と合わせて対応するとよい)

7. **全体的な配色・UIの統一**
   - 背景:薄いピンク(彩度を落とし、警告色に見えない程度に)
   - ボタン・アクセント:薄い緑(クローバーと統一)
   - リンクをボタン風のデザインに統一、文字サイズのメリハリ

### 低優先度・完了
- ~~Deviseの日本語化~~ 完了
- ~~ログイン/新規登録時のログ紐付け~~ 完了
- ~~`turbo_confirm`が発火しない問題~~ 解決(vendor配下のJSファイル不足が原因)

### 最終タスク
7. **デプロイ**
   - 本番用のRAILS_MASTER_KEY、SECRET_KEY_BASE、DB接続設定など、未検証の項目が複数ある
   - 疲労した状態でやると環境差異のトラブルで消耗しやすいため、フレッシュな状態で専用の時間を確保して着手する方針

---

## 開発時の注意点・学んだ教訓

- **タイポに注意**:`params [:x]`(余計なスペース)、`&/update`(`&.`のはずが`&/`)、`find by`(`find_by`のはずがスペース)など、エラーメッセージを丁寧に読んで、記号・スペースの位置を確認する習慣が重要
- **`git status`をこまめに確認**:意図しないファイル(`MAX_CYCLES`の一時的な変更値など)が紛れ込んでいないか、コミット前に必ず確認する
- **CSSの`position: absolute`や負の`margin`**:見えない要素が、他の要素の上に重なってクリックを妨害することがある(桜の花びらCSSで実際に発生)
- **ブラウザキャッシュ**:JS/CSSの変更が反映されない時は、まず強制リロード(Cmd+Shift+R)を試す
- **HTMLタグの閉じ忘れ**:見た目のスタイル崩れの原因になることがある(pタグの閉じ忘れでh1/h3のサイズが逆転した事例あり)
